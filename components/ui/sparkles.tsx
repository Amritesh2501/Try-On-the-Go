import React from "react";

// Dummy component to replace the tsparticles implementation
// preventing module loading errors in web environments.
export const SparklesCore = (props: any) => {
  return null;
};
